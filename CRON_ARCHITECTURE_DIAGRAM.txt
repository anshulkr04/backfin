```
╔═══════════════════════════════════════════════════════════════════════════════╗
║                    BACKFIN DAILY CRON SYSTEM ARCHITECTURE                     ║
╚═══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│                          daily_cron_manager.py                               │
│                           (APScheduler Service)                              │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               │
                ┌──────────────┴──────────────┐
                │                             │
        ┌───────▼────────┐            ┌──────▼──────┐
        │   7:00 PM      │            │  12:00 AM   │
        │ Data Collection│            │   Watchlist │
        │     (Daily)    │            │   Digest    │
        └───────┬────────┘            └──────┬──────┘
                │                             │
    ┌───────────┼───────────┐                │
    │           │           │                │
┌───▼────┐  ┌──▼───┐  ┌────▼─────┐    ┌────▼──────┐
│Corporate│  │Deals │  │ Insider  │    │   Email   │
│Actions  │  │      │  │ Trading  │    │  Sender   │
└───┬────┘  └──┬───┘  └────┬─────┘    └────┬──────┘
    │          │           │                │
    │          │           │                │
┌───▼──────────▼───────────▼────────────────▼──────────┐
│                                                       │
│                  SUPABASE DATABASE                    │
│                                                       │
│  ┌──────────────┐  ┌────────┐  ┌─────────────────┐  │
│  │ corporate_   │  │ deals  │  │ insider_trading │  │
│  │ actions      │  │        │  │                 │  │
│  └──────────────┘  └────────┘  └─────────────────┘  │
│                                                       │
│  ┌─────────────────────────────┐                     │
│  │ user_notification_queue     │                     │
│  └─────────────────────────────┘                     │
└───────────────────────────────────────────────────────┘
                               │
                               │
                    ┌──────────▼──────────┐
                    │   RESEND API        │
                    │  (Email Delivery)   │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   User Inboxes      │
                    │  📧 📧 📧 📧 📧     │
                    └─────────────────────┘


═══════════════════════════════════════════════════════════════════════════════

                            DATA FLOW DETAILS

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│                    CORPORATE ACTIONS (7:00 PM)                               │
└─────────────────────────────────────────────────────────────────────────────┘

  NSE API ────┐
              ├──► corporate_actions_collector.py ──► Normalize ──┐
  BSE API ────┘                                                     │
                                                                    ├──► Deduplicate ──► Supabase
                                                                    │    (NSE Priority)
                                                                    │
                                                    Check Existing ─┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                         DEALS (7:00 PM)                                      │
└─────────────────────────────────────────────────────────────────────────────┘

  NSE API ────────────┐
  (Bulk & Block)      ├──► deals_detector.py ──► Normalize ──┐
                      │                                        │
  BSE Selenium ───────┘                                        ├──► Deduplicate ──► Supabase
  (Headless Chrome)                                            │    (BSE Priority)
                                                               │
                                               Check Existing ─┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                    INSIDER TRADING (7:00 PM)                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  NSE API ────────────┐
                      ├──► insider_trading_detector.py ──► Parse ──┐
  BSE Selenium ───────┘                                             │
  (Download CSV)                                                    ├──► Deduplicate ──► Supabase
                                                                    │    (BSE Priority)
                                                                    │
                                                    Check Existing ─┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                     WATCHLIST DIGEST (12:00 AM)                              │
└─────────────────────────────────────────────────────────────────────────────┘

  Supabase ──► Query user_notification_queue ──┐
                                                │
  ┌───────────────────────────────────────────┘
  │
  ├──► For each user:
  │    ├─► Get pending notifications
  │    ├─► Group by company
  │    ├─► Generate HTML email
  │    ├─► Send via Resend API
  │    └─► Mark as processed
  │
  └──► Update notification_queue


═══════════════════════════════════════════════════════════════════════════════

                           MANAGEMENT COMMANDS

═══════════════════════════════════════════════════════════════════════════════

Setup & Validation:
  bash setup_cron.sh              # One-time setup
  python3 check_cron_setup.py     # Validate configuration

Service Control:
  ./manage_cron.sh start          # Start service in background
  ./manage_cron.sh stop           # Stop service
  ./manage_cron.sh restart        # Restart service
  ./manage_cron.sh status         # Check if running
  ./manage_cron.sh logs follow    # Watch logs in real-time

Testing:
  ./manage_cron.sh test corporate_actions
  ./manage_cron.sh test deals
  ./manage_cron.sh test insider_trading
  ./manage_cron.sh test watchlist_digest
  ./manage_cron.sh test all_data

Historical Backfill:
  python3 collect_historical_corporate_actions.py
  python3 collect_historical_deals.py
  python3 collect_historical_insider_trading.py


═══════════════════════════════════════════════════════════════════════════════

                              FILE STRUCTURE

═══════════════════════════════════════════════════════════════════════════════

backfin/
├── daily_cron_manager.py              # Main scheduler
├── manage_cron.sh                     # Control script
├── setup_cron.sh                      # Setup script
├── check_cron_setup.py                # Validation script
│
├── collect_historical_corporate_actions.py
├── collect_historical_deals.py
├── collect_historical_insider_trading.py
│
├── QUICK_START_CRON.md                # Quick reference
├── CRON_MANAGER_README.md             # Full documentation
├── CRON_IMPLEMENTATION_SUMMARY.md     # This summary
│
├── logs/
│   ├── cron/                          # Scheduler logs
│   │   └── cron_manager_YYYYMMDD.log
│   ├── queues/                        # Queue logs
│   ├── system/                        # System logs
│   └── workers/                       # Worker logs
│
├── src/services/exchange_data/
│   ├── corporate_actions/
│   │   └── corporate_actions_collector.py
│   ├── deals_management/
│   │   └── deals_detector.py
│   └── insider_trading/
│       └── insider_trading_detector.py
│
└── scripts/
    └── send_daily_digest.py           # Email sender


═══════════════════════════════════════════════════════════════════════════════

                           MONITORING & LOGS

═══════════════════════════════════════════════════════════════════════════════

Real-time Monitoring:
  ./manage_cron.sh status              # Service status
  ./manage_cron.sh logs follow         # Live log stream
  tail -f logs/cron/cron_manager_*.log # Direct log access

Log Locations:
  logs/cron/cron_manager_YYYYMMDD.log  # Main scheduler
  logs/cron.log                        # Background output
  logs/queues/                         # Queue processing
  logs/system/                         # System events
  logs/workers/                        # Worker processes

Error Investigation:
  grep -i error logs/cron/*.log        # Find errors
  grep -i failed logs/cron/*.log       # Find failures


═══════════════════════════════════════════════════════════════════════════════

                          SUCCESS INDICATORS

═══════════════════════════════════════════════════════════════════════════════

✅ Service Running:
   ./manage_cron.sh status shows "Service is running"

✅ Jobs Scheduled:
   Logs show "Scheduled: Data Collection at 7:00 PM daily"
   Logs show "Scheduled: Watchlist Digest at 12:00 AM daily"

✅ Data Collection Working:
   At 7:00 PM, logs show:
   - "Corporate Actions collection completed successfully"
   - "Deals collection completed successfully"
   - "Insider Trading collection completed successfully"

✅ Email Sending Working:
   At 12:00 AM, logs show:
   - "Watchlist digest emails sent successfully"
   - "Sent digest to X users"

✅ Database Updated:
   Check Supabase tables for new records after 7:00 PM


═══════════════════════════════════════════════════════════════════════════════
```
